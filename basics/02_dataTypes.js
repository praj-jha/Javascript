"use strict";  //treat all js code as newer version of js, but nowadays it by default runs on the moderns version

//alert(3+3)   we are using node not browser so it wont work this way

let name = "prajwal"
let age = "45"

let isLoggedIn = false;

//string
//null => standalone value 
//undefined => something which is declared but not assigned
//symbol => uniqness 

//object

console.log(typeof null);   //null is an object, which is maybe a issue with js itself
console.log(typeof undefined);  //type of undefined is undefined